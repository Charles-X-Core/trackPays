import { CurrencySolPipe } from './currency-sol-pipe';

describe('CurrencySolPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySolPipe();
    expect(pipe).toBeTruthy();
  });
});
